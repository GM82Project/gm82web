## Setup Update

We have a new update ready for download. You can get the new version in the Downloads tab.

```
New extension: Nimble File System
- create and manage Metafiles, which can store any number of buffers
- functions to filter, add, delete files from the metafile etc.
- way faster than saving many small files to disk
- full Johnny documentation provided with initial release

New extension by Samuel Venable: Dear ImGui Dialogs
- show Dear ImGui dialog boxes such as message boxes and file pickers
- full unicode font & localization support

gm82:
- increased the size of the background texture for larger monitors

directx9:
- fixed scaled mode point options using incorrect perspective correction

core:
- added new constants vk_oem1 (semicolon) vk_oem2 (slash) and vk_oem3 (backtick)

ui:
- new function ui_prebake_layout() to skip the first draw lag spike
```